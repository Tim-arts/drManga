<section class="modal-body-container animated animatedFadeInUp" data-form="forgot" data-step="0">
    <header class="text-center">
        <div class="modal-content-title">
            <h1 class="mt-3">Mot de passe oublié</h1>
            <div class="my-1">
                <span class="text-purple-dark">Retrouver son mot de passe</span>
            </div>
        </div>
    </header>
    <form id="fos_user_forgot_form" name="fos_user_forgot_form" action="{{ path('fos_user_resetting_send_email') }}" method="post" novalidate>
        <div class="modal-body mb-1">
            <div class="form-block form-sm mb-4">
                <input type="text" id="username" name="username" class="form-control no-border w-100 text-white bg-grey-dark" placeholder="Nom d'utilisateur ou adresse e-mail" data-type="name">
            </div>
            <div class="row mt-4">
                <button type="button" id="forgot-display-login-section" class="btn rounded high upper contains-icon mr-auto theme-3" data-section-open="login" data-section-close="forgot">
                    <span>Connexion</span>
                    <span class="icon bg-grey-darker">
                        <img src="{{ asset('img/svg/right-chevron-white.svg') }}" class="svg small vertical-align-sub" alt="Direction droite" />
                    </span>
                </button>
                <button type="submit" class="btn rounded high upper no-border contains-icon theme-2 ml-auto">
                    <span>Valider</span>
                    <span class="icon bg-grey-darker">
                        <img src="{{ asset('img/svg/right-chevron-white.svg') }}" class="svg small vertical-align-sub" alt="Direction droite" />
                    </span>
                </button>
            </div>
        </div>
    </form>
</section>
